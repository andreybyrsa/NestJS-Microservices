{
  "name": "nestjs-app-utils",
  "version": "1.1.3",
  "description": "library for nest-js project",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "author": "andreybyrsa",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.19",
    "rimraf": "^2.6.3",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@nestjs/common": "^10.3.3",
    "@nestjs/microservices": "^10.3.3",
    "@nestjs/swagger": "^7.3.0"
  }
}
